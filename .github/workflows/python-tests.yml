name: Python Tests & Type Checking

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        cd blokus-engine
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    
    - name: Run MyPy (Type Checking)
      run: |
        cd blokus-engine
        mypy src/blokus --strict --show-error-codes
      continue-on-error: true  # Don't fail build yet, just report
    
    - name: Run Tests
      run: |
        cd blokus-engine
        pytest tests/ -v --cov=src/blokus --cov-report=xml --cov-report=term
    
    - name: Upload Coverage
      uses: codecov/codecov-action@v4
      with:
        file: ./blokus-engine/coverage.xml
        flags: unittests
        name: codecov-blokus
      if: always()
